{
  "name": "workpay",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared",
    "examples/agent-buyer"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "test": "npm run test -w web && cd packages/contracts && forge test",
    "db:push": "npm run db:push -w web",
    "db:seed": "npm run db:seed -w web",
    "contracts:test": "cd packages/contracts && forge test"
  }
}
